$shell = New-Object -ComObject Shell.Application
foreach ($win in $shell.Windows()) {
    Write-Host "Name: $($win.Name)"
    Write-Host "HWND: $($win.HWND)"
    Write-Host "LocationURL: $($win.LocationURL)"
    Write-Host "LocationName: $($win.LocationName)"
    Write-Host "------"
}
